# Nifty Oracle — Python dependencies
# Install:  pip install -r requirements.txt
# Python 3.10+ recommended.

# ---- Core: data, models, dashboard (required) ----
pandas==2.2.3
numpy==2.1.3
lightgbm==4.7.0          # forecast models (8 multi-horizon classifiers)
scikit-learn==1.6.1      # GradientBoosting for the daily ORB bias model
yfinance==1.5.1          # free fallback data feed
requests==2.32.3         # AI (NVIDIA/Gemini) REST calls + NSE bhavcopy download

# ---- Angel One SmartAPI (primary live feed) ----
smartapi-python==1.5.5
pyotp==2.10.0            # generates the TOTP code for auto-login (no SMS)
logzero==1.7.0           # SmartAPI's logger (we silence it to avoid leaking creds)
websocket-client==1.8.0  # SmartWebSocketV2 live tick stream

# ---- Volatility LSTM (deep temporal model) ----
torch==2.8.0
